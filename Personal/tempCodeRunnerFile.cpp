vector<pair<int, int>> v;
    //  for(int i=0; i<n; i++)
    //  {
    //     v[i]=make_pair(ar[i],i);
    //  }
    //  sort(all(v));
    //  for(int i=0; i<n; i++) cout<<v[i].first<<" "<<v[i].second<<" ";